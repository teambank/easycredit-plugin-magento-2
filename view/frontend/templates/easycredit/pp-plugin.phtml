<?php
/**
 * @var \Netzkollektiv\EasyCredit\Block\PpPlugin $block
 */
if (!$block->getApiKey()) {
    return;
}
?>
<?php /**
       * * @var \Netzkollektiv\EasyCredit\Block\PpPlugin $block 
       */ ?>
<script>
    require([
        "jquery",
        "mage/translate",
        "domReady!",
    ], function ($, modal) {
        $(function() {
            $('.product-info-main .price-box').each(function(){
                var me = this;
                var amount = function() {
                    return $(me).find('*[data-price-type="finalPrice"]').data('price-amount');
                }();

                $(this).append(['<easycredit-widget', 
                    'webshop-id="<?php echo $block->escapeHtml($block->getApiKey()); ?>"',
                    'amount="'+amount+'"',
                '/>'].join(' '));
            });
        });
    });
</script>
